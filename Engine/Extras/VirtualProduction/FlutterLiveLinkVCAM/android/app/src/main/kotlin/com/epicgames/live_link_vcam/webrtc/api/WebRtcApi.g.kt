// Copyright Epic Games, Inc. All Rights Reserved.
// 
// This is an auto-generated file. See FlutterLiveLinkVCAM/pigeons/README for information on how to
// modify the original source and generate a new file.
// 
// Autogenerated from Pigeon (v14.0.1), do not edit directly.
// See also: https://pub.dev/packages/pigeon


import android.util.Log
import io.flutter.plugin.common.BasicMessageChannel
import io.flutter.plugin.common.BinaryMessenger
import io.flutter.plugin.common.MessageCodec
import io.flutter.plugin.common.StandardMessageCodec
import java.io.ByteArrayOutputStream
import java.nio.ByteBuffer

private fun wrapResult(result: Any?): List<Any?> {
  return listOf(result)
}

private fun wrapError(exception: Throwable): List<Any?> {
  if (exception is FlutterError) {
    return listOf(
      exception.code,
      exception.message,
      exception.details
    )
  } else {
    return listOf(
      exception.javaClass.simpleName,
      exception.toString(),
      "Cause: " + exception.cause + ", Stacktrace: " + Log.getStackTraceString(exception)
    )
  }
}

private fun createConnectionError(channelName: String): FlutterError {
  return FlutterError("channel-error",  "Unable to establish connection on channel: '$channelName'.", "")}

/**
 * Error class for passing custom error details to Flutter via a thrown PlatformException.
 * @property code The error code.
 * @property message The error message.
 * @property details The error details. Must be a datatype supported by the api codec.
 */
class FlutterError (
  val code: String,
  override val message: String? = null,
  val details: Any? = null
) : Throwable()

/** Supported types for [RtcSessionDescription]. */
enum class RtcSessionDescriptionType(val raw: Int) {
  ANSWER(0),
  OFFER(1),
  PRANSWER(2),
  ROLLBACK(3);

  companion object {
    fun ofRaw(raw: Int): RtcSessionDescriptionType? {
      return values().firstOrNull { it.raw == raw }
    }
  }
}

/** State of an [RtcPeerConnection]'s connection to the corresponding peer. */
enum class RtcPeerConnectionState(val raw: Int) {
  NEWCONNECTION(0),
  CONNECTING(1),
  CONNECTED(2),
  DISCONNECTED(3),
  FAILED(4),
  CLOSED(5);

  companion object {
    fun ofRaw(raw: Int): RtcPeerConnectionState? {
      return values().firstOrNull { it.raw == raw }
    }
  }
}

/** Kinds of MediaStreamTrack that can be created. */
enum class RtcMediaStreamTrackKind(val raw: Int) {
  AUDIO(0),
  VIDEO(1);

  companion object {
    fun ofRaw(raw: Int): RtcMediaStreamTrackKind? {
      return values().firstOrNull { it.raw == raw }
    }
  }
}

/** State of an [RtcDataChannel]. */
enum class RtcDataChannelState(val raw: Int) {
  CONNECTING(0),
  OPEN(1),
  CLOSING(2),
  CLOSED(3);

  companion object {
    fun ofRaw(raw: Int): RtcDataChannelState? {
      return values().firstOrNull { it.raw == raw }
    }
  }
}

/**
 * A buffer passed from/to WebRTC, which may contain either binary or text data.
 *
 * Generated class from Pigeon that represents data sent in messages.
 */
data class RtcDataBuffer (
  /** The binary data contained in the buffer. */
  val data: ByteArray,
  /** True if the buffer contains binary data; otherwise, it contains UTF-8 text. */
  val bIsBinary: Boolean

) {
  companion object {
    @Suppress("UNCHECKED_CAST")
    fun fromList(list: List<Any?>): RtcDataBuffer {
      val data = list[0] as ByteArray
      val bIsBinary = list[1] as Boolean
      return RtcDataBuffer(data, bIsBinary)
    }
  }
  fun toList(): List<Any?> {
    return listOf<Any?>(
      data,
      bIsBinary,
    )
  }
}

/**
 * Describes the configuration of one end of a peer-to-peer WebRtc connection.
 *
 * Generated class from Pigeon that represents data sent in messages.
 */
data class RtcSessionDescription (
  /** The type of description. */
  val type: RtcSessionDescriptionType,
  /** The SDP information describing the session. */
  val sdp: String

) {
  companion object {
    @Suppress("UNCHECKED_CAST")
    fun fromList(list: List<Any?>): RtcSessionDescription {
      val type = RtcSessionDescriptionType.ofRaw(list[0] as Int)!!
      val sdp = list[1] as String
      return RtcSessionDescription(type, sdp)
    }
  }
  fun toList(): List<Any?> {
    return listOf<Any?>(
      type.raw,
      sdp,
    )
  }
}

/**
 * A candidate for interactive connectivity establishment (ICE) to a peer.
 *
 * Generated class from Pigeon that represents data sent in messages.
 */
data class RtcIceCandidate (
  /** A string describing the candidate (taken from the SDP "candidate" field). */
  val candidate: String,
  /** A string specifying the candidate's unique media stream ID tag. */
  val sdpMid: String,
  /** The index number of the media description within the SDP. */
  val sdpMLineIndex: Long

) {
  companion object {
    @Suppress("UNCHECKED_CAST")
    fun fromList(list: List<Any?>): RtcIceCandidate {
      val candidate = list[0] as String
      val sdpMid = list[1] as String
      val sdpMLineIndex = list[2].let { if (it is Int) it.toLong() else it as Long }
      return RtcIceCandidate(candidate, sdpMid, sdpMLineIndex)
    }
  }
  fun toList(): List<Any?> {
    return listOf<Any?>(
      candidate,
      sdpMid,
      sdpMLineIndex,
    )
  }
}

/**
 * Event data for when a track is added to a stream in a [RtcPeerConnection].
 *
 * Generated class from Pigeon that represents data sent in messages.
 */
data class RtcTrackEvent (
  /** The unique ID of the media track used to refer to it when communicating with the native plugin. */
  val trackId: Long,
  /** The kind of media in this track. */
  val kind: RtcMediaStreamTrackKind

) {
  companion object {
    @Suppress("UNCHECKED_CAST")
    fun fromList(list: List<Any?>): RtcTrackEvent {
      val trackId = list[0].let { if (it is Int) it.toLong() else it as Long }
      val kind = RtcMediaStreamTrackKind.ofRaw(list[1] as Int)!!
      return RtcTrackEvent(trackId, kind)
    }
  }
  fun toList(): List<Any?> {
    return listOf<Any?>(
      trackId,
      kind.raw,
    )
  }
}

/**
 * Reported statistics about a single object inspected as part of a WebRTC report.
 *
 * Generated class from Pigeon that represents data sent in messages.
 */
data class RtcStats (
  /** Timestamp at which the stats were gathered (in microseconds). */
  val timestampUs: Double,
  /** The type of object for which these stats were collected. */
  val type: String,
  /** The unique ID of the stats object. */
  val id: String,
  /** Map from stat name to its value. */
  val values: Map<String?, Any?>

) {
  companion object {
    @Suppress("UNCHECKED_CAST")
    fun fromList(list: List<Any?>): RtcStats {
      val timestampUs = list[0] as Double
      val type = list[1] as String
      val id = list[2] as String
      val values = list[3] as Map<String?, Any?>
      return RtcStats(timestampUs, type, id, values)
    }
  }
  fun toList(): List<Any?> {
    return listOf<Any?>(
      timestampUs,
      type,
      id,
      values,
    )
  }
}

/**
 * A collection of reported statistics gathered about a WebRTC peer connection.
 *
 * Generated class from Pigeon that represents data sent in messages.
 */
data class RtcStatsReport (
  /** Timestamp at which the stats were gathered (in microseconds). */
  val timestampUs: Double,
  /** Map from object name to the stats associated with it. */
  val stats: Map<String?, RtcStats?>

) {
  companion object {
    @Suppress("UNCHECKED_CAST")
    fun fromList(list: List<Any?>): RtcStatsReport {
      val timestampUs = list[0] as Double
      val stats = list[1] as Map<String?, RtcStats?>
      return RtcStatsReport(timestampUs, stats)
    }
  }
  fun toList(): List<Any?> {
    return listOf<Any?>(
      timestampUs,
      stats,
    )
  }
}

@Suppress("UNCHECKED_CAST")
private object RtcPeerConnectionHostApiCodec : StandardMessageCodec() {
  override fun readValueOfType(type: Byte, buffer: ByteBuffer): Any? {
    return when (type) {
      128.toByte() -> {
        return (readValue(buffer) as? List<Any?>)?.let {
          RtcIceCandidate.fromList(it)
        }
      }
      129.toByte() -> {
        return (readValue(buffer) as? List<Any?>)?.let {
          RtcSessionDescription.fromList(it)
        }
      }
      130.toByte() -> {
        return (readValue(buffer) as? List<Any?>)?.let {
          RtcStats.fromList(it)
        }
      }
      131.toByte() -> {
        return (readValue(buffer) as? List<Any?>)?.let {
          RtcStatsReport.fromList(it)
        }
      }
      else -> super.readValueOfType(type, buffer)
    }
  }
  override fun writeValue(stream: ByteArrayOutputStream, value: Any?)   {
    when (value) {
      is RtcIceCandidate -> {
        stream.write(128)
        writeValue(stream, value.toList())
      }
      is RtcSessionDescription -> {
        stream.write(129)
        writeValue(stream, value.toList())
      }
      is RtcStats -> {
        stream.write(130)
        writeValue(stream, value.toList())
      }
      is RtcStatsReport -> {
        stream.write(131)
        writeValue(stream, value.toList())
      }
      else -> super.writeValue(stream, value)
    }
  }
}

/**
 * API that receives messages about RtcPeerConnection instances in the host language.
 *
 * Generated interface from Pigeon that represents a handler of messages from Flutter.
 */
interface RtcPeerConnectionHostApi {
  /** Set the field trial keys and values enabled for future PeerConnections. */
  fun setFieldTrials(fieldTrials: Map<String, String>?)
  /**
   * Create a PeerConnection.
   * Returns an ID which can be used to refer to the native object in future calls.
   */
  fun create(): Long
  /** Dispose of the PeerConnection with the given [connectionId]. */
  fun dispose(connectionId: Long)
  /** Set the [description] of the remote peer for the PeerConnection with the given [connectionId]. */
  fun setRemoteDescription(connectionId: Long, description: RtcSessionDescription, callback: (Result<Unit>) -> Unit)
  /** Set the [description] of the local peer for the PeerConnection with the given [connectionId]. */
  fun setLocalDescription(connectionId: Long, description: RtcSessionDescription, callback: (Result<Unit>) -> Unit)
  /** Add an ICE [candidate] for connecting to the remote peer. */
  fun addRemoteCandidate(connectionId: Long, candidate: RtcIceCandidate, callback: (Result<Unit>) -> Unit)
  /** Create an answer to a WebRTC offer for the PeerConnection with the given [connectionId]. */
  fun createAnswer(connectionId: Long, callback: (Result<RtcSessionDescription>) -> Unit)
  /**
   * Generate a stats report about the PeerConnection with the given [connectionId].
   * If [typeFilter] is provided, the report will only contain stats matching types in that list, reducing the codec
   * overhead from the native platform.
   */
  fun getStats(connectionId: Long, typeFilter: List<String>?, callback: (Result<RtcStatsReport>) -> Unit)

  companion object {
    /** The codec used by RtcPeerConnectionHostApi. */
    val codec: MessageCodec<Any?> by lazy {
      RtcPeerConnectionHostApiCodec
    }
    /** Sets up an instance of `RtcPeerConnectionHostApi` to handle messages through the `binaryMessenger`. */
    @Suppress("UNCHECKED_CAST")
    fun setUp(binaryMessenger: BinaryMessenger, api: RtcPeerConnectionHostApi?) {
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.live_link_vcam.RtcPeerConnectionHostApi.setFieldTrials", codec)
        if (api != null) {
          channel.setMessageHandler { message, reply ->
            val args = message as List<Any?>
            val fieldTrialsArg = args[0] as Map<String, String>?
            var wrapped: List<Any?>
            try {
              api.setFieldTrials(fieldTrialsArg)
              wrapped = listOf<Any?>(null)
            } catch (exception: Throwable) {
              wrapped = wrapError(exception)
            }
            reply.reply(wrapped)
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.live_link_vcam.RtcPeerConnectionHostApi.create", codec)
        if (api != null) {
          channel.setMessageHandler { _, reply ->
            var wrapped: List<Any?>
            try {
              wrapped = listOf<Any?>(api.create())
            } catch (exception: Throwable) {
              wrapped = wrapError(exception)
            }
            reply.reply(wrapped)
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.live_link_vcam.RtcPeerConnectionHostApi.dispose", codec)
        if (api != null) {
          channel.setMessageHandler { message, reply ->
            val args = message as List<Any?>
            val connectionIdArg = args[0].let { if (it is Int) it.toLong() else it as Long }
            var wrapped: List<Any?>
            try {
              api.dispose(connectionIdArg)
              wrapped = listOf<Any?>(null)
            } catch (exception: Throwable) {
              wrapped = wrapError(exception)
            }
            reply.reply(wrapped)
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.live_link_vcam.RtcPeerConnectionHostApi.setRemoteDescription", codec)
        if (api != null) {
          channel.setMessageHandler { message, reply ->
            val args = message as List<Any?>
            val connectionIdArg = args[0].let { if (it is Int) it.toLong() else it as Long }
            val descriptionArg = args[1] as RtcSessionDescription
            api.setRemoteDescription(connectionIdArg, descriptionArg) { result: Result<Unit> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(wrapError(error))
              } else {
                reply.reply(wrapResult(null))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.live_link_vcam.RtcPeerConnectionHostApi.setLocalDescription", codec)
        if (api != null) {
          channel.setMessageHandler { message, reply ->
            val args = message as List<Any?>
            val connectionIdArg = args[0].let { if (it is Int) it.toLong() else it as Long }
            val descriptionArg = args[1] as RtcSessionDescription
            api.setLocalDescription(connectionIdArg, descriptionArg) { result: Result<Unit> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(wrapError(error))
              } else {
                reply.reply(wrapResult(null))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.live_link_vcam.RtcPeerConnectionHostApi.addRemoteCandidate", codec)
        if (api != null) {
          channel.setMessageHandler { message, reply ->
            val args = message as List<Any?>
            val connectionIdArg = args[0].let { if (it is Int) it.toLong() else it as Long }
            val candidateArg = args[1] as RtcIceCandidate
            api.addRemoteCandidate(connectionIdArg, candidateArg) { result: Result<Unit> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(wrapError(error))
              } else {
                reply.reply(wrapResult(null))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.live_link_vcam.RtcPeerConnectionHostApi.createAnswer", codec)
        if (api != null) {
          channel.setMessageHandler { message, reply ->
            val args = message as List<Any?>
            val connectionIdArg = args[0].let { if (it is Int) it.toLong() else it as Long }
            api.createAnswer(connectionIdArg) { result: Result<RtcSessionDescription> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(wrapError(error))
              } else {
                val data = result.getOrNull()
                reply.reply(wrapResult(data))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.live_link_vcam.RtcPeerConnectionHostApi.getStats", codec)
        if (api != null) {
          channel.setMessageHandler { message, reply ->
            val args = message as List<Any?>
            val connectionIdArg = args[0].let { if (it is Int) it.toLong() else it as Long }
            val typeFilterArg = args[1] as List<String>?
            api.getStats(connectionIdArg, typeFilterArg) { result: Result<RtcStatsReport> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(wrapError(error))
              } else {
                val data = result.getOrNull()
                reply.reply(wrapResult(data))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
    }
  }
}
@Suppress("UNCHECKED_CAST")
private object RtcPeerConnectionFlutterApiCodec : StandardMessageCodec() {
  override fun readValueOfType(type: Byte, buffer: ByteBuffer): Any? {
    return when (type) {
      128.toByte() -> {
        return (readValue(buffer) as? List<Any?>)?.let {
          RtcIceCandidate.fromList(it)
        }
      }
      129.toByte() -> {
        return (readValue(buffer) as? List<Any?>)?.let {
          RtcTrackEvent.fromList(it)
        }
      }
      else -> super.readValueOfType(type, buffer)
    }
  }
  override fun writeValue(stream: ByteArrayOutputStream, value: Any?)   {
    when (value) {
      is RtcIceCandidate -> {
        stream.write(128)
        writeValue(stream, value.toList())
      }
      is RtcTrackEvent -> {
        stream.write(129)
        writeValue(stream, value.toList())
      }
      else -> super.writeValue(stream, value)
    }
  }
}

/**
 * API that receives messages about RtcPeerConnection instances in Flutter.
 *
 * Generated class from Pigeon that represents Flutter messages that can be called from Kotlin.
 */
@Suppress("UNCHECKED_CAST")
class RtcPeerConnectionFlutterApi(private val binaryMessenger: BinaryMessenger) {
  companion object {
    /** The codec used by RtcPeerConnectionFlutterApi. */
    val codec: MessageCodec<Any?> by lazy {
      RtcPeerConnectionFlutterApiCodec
    }
  }
  /** Called when a new local ICE [candidate] is found for the PeerConnection with the given [connectionId]. */
  fun onIceCandidate(connectionIdArg: Long, candidateArg: RtcIceCandidate?, callback: (Result<Unit>) -> Unit) {
    val channelName = "dev.flutter.pigeon.live_link_vcam.RtcPeerConnectionFlutterApi.onIceCandidate"
    val channel = BasicMessageChannel<Any?>(binaryMessenger, channelName, codec)
    channel.send(listOf(connectionIdArg, candidateArg)) {
      if (it is List<*>) {
        if (it.size > 1) {
          callback(Result.failure(FlutterError(it[0] as String, it[1] as String, it[2] as String?)))
        } else {
          callback(Result.success(Unit))
        }
      } else {
        callback(Result.failure(createConnectionError(channelName)))
      } 
    }
  }
  /** Called when the [state] of the PeerConnection with the given [connectionId] changes. */
  fun onStateChanged(connectionIdArg: Long, stateArg: RtcPeerConnectionState, callback: (Result<Unit>) -> Unit) {
    val channelName = "dev.flutter.pigeon.live_link_vcam.RtcPeerConnectionFlutterApi.onStateChanged"
    val channel = BasicMessageChannel<Any?>(binaryMessenger, channelName, codec)
    channel.send(listOf(connectionIdArg, stateArg.raw)) {
      if (it is List<*>) {
        if (it.size > 1) {
          callback(Result.failure(FlutterError(it[0] as String, it[1] as String, it[2] as String?)))
        } else {
          callback(Result.success(Unit))
        }
      } else {
        callback(Result.failure(createConnectionError(channelName)))
      } 
    }
  }
  /**
   * Called when a MediaStreamTrack (described in [event]) is added to the PeerConnection with the given
   * [connectionId].
   */
  fun onTrack(connectionIdArg: Long, eventArg: RtcTrackEvent, callback: (Result<Unit>) -> Unit) {
    val channelName = "dev.flutter.pigeon.live_link_vcam.RtcPeerConnectionFlutterApi.onTrack"
    val channel = BasicMessageChannel<Any?>(binaryMessenger, channelName, codec)
    channel.send(listOf(connectionIdArg, eventArg)) {
      if (it is List<*>) {
        if (it.size > 1) {
          callback(Result.failure(FlutterError(it[0] as String, it[1] as String, it[2] as String?)))
        } else {
          callback(Result.success(Unit))
        }
      } else {
        callback(Result.failure(createConnectionError(channelName)))
      } 
    }
  }
  /**
   * Called when a DataChannel with the given [dataChannelId] is added to the PeerConnection with the given
   * [connectionId].
   */
  fun onDataChannel(connectionIdArg: Long, dataChannelIdArg: Long, callback: (Result<Unit>) -> Unit) {
    val channelName = "dev.flutter.pigeon.live_link_vcam.RtcPeerConnectionFlutterApi.onDataChannel"
    val channel = BasicMessageChannel<Any?>(binaryMessenger, channelName, codec)
    channel.send(listOf(connectionIdArg, dataChannelIdArg)) {
      if (it is List<*>) {
        if (it.size > 1) {
          callback(Result.failure(FlutterError(it[0] as String, it[1] as String, it[2] as String?)))
        } else {
          callback(Result.success(Unit))
        }
      } else {
        callback(Result.failure(createConnectionError(channelName)))
      } 
    }
  }
}
@Suppress("UNCHECKED_CAST")
private object RtcDataChannelHostApiCodec : StandardMessageCodec() {
  override fun readValueOfType(type: Byte, buffer: ByteBuffer): Any? {
    return when (type) {
      128.toByte() -> {
        return (readValue(buffer) as? List<Any?>)?.let {
          RtcDataBuffer.fromList(it)
        }
      }
      else -> super.readValueOfType(type, buffer)
    }
  }
  override fun writeValue(stream: ByteArrayOutputStream, value: Any?)   {
    when (value) {
      is RtcDataBuffer -> {
        stream.write(128)
        writeValue(stream, value.toList())
      }
      else -> super.writeValue(stream, value)
    }
  }
}

/**
 * API that receives messages about RtcDataChannel instances in the host language.
 *
 * Generated interface from Pigeon that represents a handler of messages from Flutter.
 */
interface RtcDataChannelHostApi {
  /** Send a message contained in [buffer] on the data channel with the given [dataChannelId]. */
  fun sendMessage(dataChannelId: Long, buffer: RtcDataBuffer)

  companion object {
    /** The codec used by RtcDataChannelHostApi. */
    val codec: MessageCodec<Any?> by lazy {
      RtcDataChannelHostApiCodec
    }
    /** Sets up an instance of `RtcDataChannelHostApi` to handle messages through the `binaryMessenger`. */
    @Suppress("UNCHECKED_CAST")
    fun setUp(binaryMessenger: BinaryMessenger, api: RtcDataChannelHostApi?) {
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.live_link_vcam.RtcDataChannelHostApi.sendMessage", codec)
        if (api != null) {
          channel.setMessageHandler { message, reply ->
            val args = message as List<Any?>
            val dataChannelIdArg = args[0].let { if (it is Int) it.toLong() else it as Long }
            val bufferArg = args[1] as RtcDataBuffer
            var wrapped: List<Any?>
            try {
              api.sendMessage(dataChannelIdArg, bufferArg)
              wrapped = listOf<Any?>(null)
            } catch (exception: Throwable) {
              wrapped = wrapError(exception)
            }
            reply.reply(wrapped)
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
    }
  }
}
@Suppress("UNCHECKED_CAST")
private object RtcDataChannelFlutterApiCodec : StandardMessageCodec() {
  override fun readValueOfType(type: Byte, buffer: ByteBuffer): Any? {
    return when (type) {
      128.toByte() -> {
        return (readValue(buffer) as? List<Any?>)?.let {
          RtcDataBuffer.fromList(it)
        }
      }
      else -> super.readValueOfType(type, buffer)
    }
  }
  override fun writeValue(stream: ByteArrayOutputStream, value: Any?)   {
    when (value) {
      is RtcDataBuffer -> {
        stream.write(128)
        writeValue(stream, value.toList())
      }
      else -> super.writeValue(stream, value)
    }
  }
}

/**
 * API that receives messages about RtcDataChannel instances in Flutter.
 *
 * Generated class from Pigeon that represents Flutter messages that can be called from Kotlin.
 */
@Suppress("UNCHECKED_CAST")
class RtcDataChannelFlutterApi(private val binaryMessenger: BinaryMessenger) {
  companion object {
    /** The codec used by RtcDataChannelFlutterApi. */
    val codec: MessageCodec<Any?> by lazy {
      RtcDataChannelFlutterApiCodec
    }
  }
  /** Called when the [state] of the DataChannel with the given [dataChannelId] changes. */
  fun onStateChanged(dataChannelIdArg: Long, stateArg: RtcDataChannelState, callback: (Result<Unit>) -> Unit) {
    val channelName = "dev.flutter.pigeon.live_link_vcam.RtcDataChannelFlutterApi.onStateChanged"
    val channel = BasicMessageChannel<Any?>(binaryMessenger, channelName, codec)
    channel.send(listOf(dataChannelIdArg, stateArg.raw)) {
      if (it is List<*>) {
        if (it.size > 1) {
          callback(Result.failure(FlutterError(it[0] as String, it[1] as String, it[2] as String?)))
        } else {
          callback(Result.success(Unit))
        }
      } else {
        callback(Result.failure(createConnectionError(channelName)))
      } 
    }
  }
  /** Called when a message contained in [buffer] is received on the data channel with the given [dataChannelId]. */
  fun onMessage(dataChannelIdArg: Long, bufferArg: RtcDataBuffer, callback: (Result<Unit>) -> Unit) {
    val channelName = "dev.flutter.pigeon.live_link_vcam.RtcDataChannelFlutterApi.onMessage"
    val channel = BasicMessageChannel<Any?>(binaryMessenger, channelName, codec)
    channel.send(listOf(dataChannelIdArg, bufferArg)) {
      if (it is List<*>) {
        if (it.size > 1) {
          callback(Result.failure(FlutterError(it[0] as String, it[1] as String, it[2] as String?)))
        } else {
          callback(Result.success(Unit))
        }
      } else {
        callback(Result.failure(createConnectionError(channelName)))
      } 
    }
  }
}
/**
 * API that receives messages about VideoViewController instances in the host language.
 *
 * Generated interface from Pigeon that represents a handler of messages from Flutter.
 */
interface RtcVideoViewControllerHostApi {
  /**
   * Create a VideoViewController.
   * Returns an ID which can be used to refer to the native object in future calls.
   */
  fun create(): Long
  /**
   * Set the video track of the VideoViewController with the given [controllerId] to the track with the given
   * [trackId].
   */
  fun setTrack(controllerId: Long, trackId: Long)
  /** Get the texture ID associated with the VideoViewController with the given [controllerId]. */
  fun getTextureId(controllerId: Long): Long
  /** Clear the image of the VideoViewController with the given [controllerId]. */
  fun clear(controllerId: Long)
  /** Dispose of the VideoViewController with the given [controllerId]. */
  fun dispose(controllerId: Long)

  companion object {
    /** The codec used by RtcVideoViewControllerHostApi. */
    val codec: MessageCodec<Any?> by lazy {
      StandardMessageCodec()
    }
    /** Sets up an instance of `RtcVideoViewControllerHostApi` to handle messages through the `binaryMessenger`. */
    @Suppress("UNCHECKED_CAST")
    fun setUp(binaryMessenger: BinaryMessenger, api: RtcVideoViewControllerHostApi?) {
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.live_link_vcam.RtcVideoViewControllerHostApi.create", codec)
        if (api != null) {
          channel.setMessageHandler { _, reply ->
            var wrapped: List<Any?>
            try {
              wrapped = listOf<Any?>(api.create())
            } catch (exception: Throwable) {
              wrapped = wrapError(exception)
            }
            reply.reply(wrapped)
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.live_link_vcam.RtcVideoViewControllerHostApi.setTrack", codec)
        if (api != null) {
          channel.setMessageHandler { message, reply ->
            val args = message as List<Any?>
            val controllerIdArg = args[0].let { if (it is Int) it.toLong() else it as Long }
            val trackIdArg = args[1].let { if (it is Int) it.toLong() else it as Long }
            var wrapped: List<Any?>
            try {
              api.setTrack(controllerIdArg, trackIdArg)
              wrapped = listOf<Any?>(null)
            } catch (exception: Throwable) {
              wrapped = wrapError(exception)
            }
            reply.reply(wrapped)
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.live_link_vcam.RtcVideoViewControllerHostApi.getTextureId", codec)
        if (api != null) {
          channel.setMessageHandler { message, reply ->
            val args = message as List<Any?>
            val controllerIdArg = args[0].let { if (it is Int) it.toLong() else it as Long }
            var wrapped: List<Any?>
            try {
              wrapped = listOf<Any?>(api.getTextureId(controllerIdArg))
            } catch (exception: Throwable) {
              wrapped = wrapError(exception)
            }
            reply.reply(wrapped)
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.live_link_vcam.RtcVideoViewControllerHostApi.clear", codec)
        if (api != null) {
          channel.setMessageHandler { message, reply ->
            val args = message as List<Any?>
            val controllerIdArg = args[0].let { if (it is Int) it.toLong() else it as Long }
            var wrapped: List<Any?>
            try {
              api.clear(controllerIdArg)
              wrapped = listOf<Any?>(null)
            } catch (exception: Throwable) {
              wrapped = wrapError(exception)
            }
            reply.reply(wrapped)
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.live_link_vcam.RtcVideoViewControllerHostApi.dispose", codec)
        if (api != null) {
          channel.setMessageHandler { message, reply ->
            val args = message as List<Any?>
            val controllerIdArg = args[0].let { if (it is Int) it.toLong() else it as Long }
            var wrapped: List<Any?>
            try {
              api.dispose(controllerIdArg)
              wrapped = listOf<Any?>(null)
            } catch (exception: Throwable) {
              wrapped = wrapError(exception)
            }
            reply.reply(wrapped)
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
    }
  }
}
/**
 * API that receives messages about VideoViewController instances in Flutter.
 *
 * Generated class from Pigeon that represents Flutter messages that can be called from Kotlin.
 */
@Suppress("UNCHECKED_CAST")
class RtcVideoViewControllerFlutterApi(private val binaryMessenger: BinaryMessenger) {
  companion object {
    /** The codec used by RtcVideoViewControllerFlutterApi. */
    val codec: MessageCodec<Any?> by lazy {
      StandardMessageCodec()
    }
  }
  /**
   * Called when the VideoViewController with the given [controllerId] has rendered its first frame since it was
   * created or cleared (whichever is latest).
   */
  fun onFirstFrameRendered(controllerIdArg: Long, callback: (Result<Unit>) -> Unit) {
    val channelName = "dev.flutter.pigeon.live_link_vcam.RtcVideoViewControllerFlutterApi.onFirstFrameRendered"
    val channel = BasicMessageChannel<Any?>(binaryMessenger, channelName, codec)
    channel.send(listOf(controllerIdArg)) {
      if (it is List<*>) {
        if (it.size > 1) {
          callback(Result.failure(FlutterError(it[0] as String, it[1] as String, it[2] as String?)))
        } else {
          callback(Result.success(Unit))
        }
      } else {
        callback(Result.failure(createConnectionError(channelName)))
      } 
    }
  }
  /** Called when the size of a VideoViewController's frame size changes. */
  fun onFrameSizeChanged(controllerIdArg: Long, widthArg: Long, heightArg: Long, callback: (Result<Unit>) -> Unit) {
    val channelName = "dev.flutter.pigeon.live_link_vcam.RtcVideoViewControllerFlutterApi.onFrameSizeChanged"
    val channel = BasicMessageChannel<Any?>(binaryMessenger, channelName, codec)
    channel.send(listOf(controllerIdArg, widthArg, heightArg)) {
      if (it is List<*>) {
        if (it.size > 1) {
          callback(Result.failure(FlutterError(it[0] as String, it[1] as String, it[2] as String?)))
        } else {
          callback(Result.success(Unit))
        }
      } else {
        callback(Result.failure(createConnectionError(channelName)))
      } 
    }
  }
}
