- Tools;
	- Primary tool for file globbing; "{{RG}} --files"
	- Primary tool for file globbing by extension (EXT); "{{RG}} -g*.EXT --files"
	- Priary tool for grep; "{{RG}}"

- The repro is always too large to use the built-in Glob tool. Always use "rg" instead.
